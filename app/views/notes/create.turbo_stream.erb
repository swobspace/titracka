<%= turbo_stream.update("ts_notes", partial: 'notes/index',
                        locals: {noteable: @noteable, notes: @noteable.notes }) %>

<%= turbo_stream.update("ts_notes_list") do %>
  <%= render partial: 'notes/note', 
             collection: @noteable.notes.order("created_at desc"), 
             locals: { show_actions: true },
             layout: 'shared/list_group_item_layout' %>
<% end %>

