<%= render 'shared/turbo-cache-control' %>

<h1><%= t('controller.tasks') %></h1>

<div id="ts_tasks" class="pt-2">
  <%= render 'index', tasks: @tasks %>
</div>

<br />

<%= back_link %>
<%= new_link([@taskable, Task],
             data: { 'turbo-frame': "modal" })
%>
